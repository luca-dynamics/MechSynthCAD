import { formatNumber, formatVector } from "@/lib/format";
import type { SliderCrankAnalysisResult } from "@/types";

type Props = { error: string | null; result: SliderCrankAnalysisResult | null };

export function SliderCrankResultsPanel({ error, result }: Props) {
  return <aside className="rounded-2xl border border-slate-300 bg-white p-5 shadow-sm"><h2 className="text-xl font-semibold">Slider-Crank Results</h2><p className="mt-1 text-sm text-slate-500">Structured deterministic solver output.</p>{error && <div className="mt-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}{result && <div className="mt-4 rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm text-slate-700"><h3 className="font-semibold text-slate-900">Analysis Summary</h3><dl className="mt-3 grid grid-cols-2 gap-x-4 gap-y-2"><dt className="font-medium">Valid</dt><dd>{String(result.valid)}</dd><dt className="font-medium">θ</dt><dd>{formatNumber(result.theta_deg, "°")}</dd><dt className="font-medium">Slider position</dt><dd>{formatNumber(result.slider_position, " mm")}</dd><dt className="font-medium">Transmission angle</dt><dd>{formatNumber(result.transmission_angle_deg, "°")}</dd></dl><h4 className="mt-4 font-semibold text-slate-900">Velocity</h4><dl className="mt-2 grid grid-cols-2 gap-x-4 gap-y-2"><dt>V_A</dt><dd>{formatVector(result.velocity_analysis.velocity_A, " mm/s")}</dd><dt>V_B</dt><dd>{formatVector(result.velocity_analysis.velocity_B, " mm/s")}</dd><dt>Slider velocity</dt><dd>{formatNumber(result.velocity_analysis.slider_velocity, " mm/s")}</dd></dl><h4 className="mt-4 font-semibold text-slate-900">Acceleration</h4><dl className="mt-2 grid grid-cols-2 gap-x-4 gap-y-2"><dt>A_A</dt><dd>{formatVector(result.acceleration_analysis.acceleration_A, " mm/s²")}</dd><dt>A_B</dt><dd>{formatVector(result.acceleration_analysis.acceleration_B, " mm/s²")}</dd><dt>Slider acceleration</dt><dd>{formatNumber(result.acceleration_analysis.slider_acceleration, " mm/s²")}</dd></dl></div>}<pre className="mt-4 min-h-[320px] overflow-auto rounded-xl bg-slate-950 p-4 text-sm text-slate-100">{result ? JSON.stringify(result, null, 2) : "Run slider-crank analysis to display the API response."}</pre></aside>;
}
