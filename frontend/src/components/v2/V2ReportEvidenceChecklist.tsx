import type { V2ReportEvidenceItem } from "@/components/v2/academicReportBuilder";

export function V2ReportEvidenceChecklist({ items, onCommand }: { items: V2ReportEvidenceItem[]; onCommand: (command: string) => void }) {
  return <section className="rounded-2xl border border-v2-border bg-[#0d0c0b] p-4"><p className="text-[11px] font-bold uppercase tracking-[0.22em] text-amber-500">Evidence checklist</p><div className="mt-3 grid gap-2 md:grid-cols-2">{items.map((item) => <div key={item.id} className="rounded-xl border border-v2-border bg-[#11100f] p-3"><div className="flex flex-wrap items-start justify-between gap-2"><h5 className="text-sm font-semibold text-v2-text">{item.label}</h5><span className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase tracking-[0.12em] ${item.ready ? "bg-emerald-500/15 text-emerald-300" : "bg-amber-500/15 text-amber-300"}`}>{item.ready ? "Ready" : "Missing"}</span></div><p className="mt-2 text-xs leading-5 text-v2-muted">{item.explanation}</p>{item.actionLabel ? <button type="button" onClick={() => item.actionCommand && onCommand(item.actionCommand)} className="mt-3 rounded-lg border border-v2-border bg-[#191715] px-3 py-2 text-xs font-semibold text-v2-muted hover:text-amber-500">{item.actionLabel}</button> : null}</div>)}</div></section>;
}
