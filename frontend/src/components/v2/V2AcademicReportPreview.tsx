import type { V2AcademicReport } from "@/components/v2/academicReportBuilder";

export function V2AcademicReportPreview({ report }: { report: V2AcademicReport }) {
  return <section className="rounded-2xl border border-v2-border bg-[#0d0c0b] p-2 sm:p-4"><div className="mx-auto max-w-4xl rounded-sm bg-neutral-50 p-5 text-slate-950 shadow-2xl sm:p-8 print-report-area" data-print-report="true"><div className="border-b border-slate-300 pb-6 text-center"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-slate-600">Academic Project Report</p><h1 className="mt-4 text-2xl font-bold leading-tight">{report.title}</h1></div><div className="mt-6 space-y-6">{report.sections.map((section) => <article key={section.heading} className="break-words"><h2 className="border-b border-slate-200 pb-1 text-lg font-bold text-slate-950">{section.heading}</h2><p className="mt-2 whitespace-pre-wrap text-sm leading-7 text-slate-800">{section.content}</p>{section.rows ? <div className="mt-3 overflow-x-auto"><table className="w-full border-collapse text-left text-xs"><tbody>{section.rows.map(([k, v]) => <tr key={`${section.heading}-${k}`}><th className="w-1/3 border border-slate-300 bg-slate-100 px-2 py-2 font-semibold">{k}</th><td className="border border-slate-300 px-2 py-2 font-mono text-[11px]">{v}</td></tr>)}</tbody></table></div> : null}{section.note ? <p className="mt-2 rounded border border-amber-200 bg-amber-50 p-2 text-xs text-amber-900">{section.note}</p> : null}</article>)}</div></div></section>;
}
